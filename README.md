run `npm start` in terminal to run the chat server.

run `npm run client` in terminal to run the chat client.

MessageBus.onIncomingMessage has some thoughts and ideas 
I have about the profanity words.
